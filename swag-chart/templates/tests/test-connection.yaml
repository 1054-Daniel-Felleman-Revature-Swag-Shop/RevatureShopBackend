apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "swag-chart.fullname" . }}-test-connection"
  labels:
    {{- include "swag-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "swag-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
